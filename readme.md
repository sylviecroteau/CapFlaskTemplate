Latest update requires to new packages: certifi and an update of flask-moment.

run the following commands in the terminal: <br>
python -m pip install flask-moment -U <br>
python -m pip install certifi
